type Profile @model @key(name: "byUsername", fields: ["username", "id"], queryField: "profilesByUser") @auth(rules: [{allow: private, operations: [read]}, {allow: owner, operations: [read, update, create]}]) {
  id: ID!
  username: String!
  djname: String
  genre: String
}
 