type Profile @model
@auth(rules: [
  {allow: private, operations: [read, update, create]},
  {allow: owner, operations: [read]}])
@key(name: "byUsername", fields: ["username", "id"], queryField: "profilesByUser")
{
  id: ID!
  username: String!
  djname: String
  genre: String
}
 