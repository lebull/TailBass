type Profile @model @key(name: "byOwner", fields: ["owner"], queryField: "profilesByOwner") @auth(rules: [{allow: private, operations: [read]}, {allow: owner, ownerField: "owner", operations: [create, update, read] }]) {
  id: ID!
  owner: String!
  djname: String
  genre: String
}
 